export const environment = {
  production: true,
  // Set this to your deployed backend's base URL after deploying it (e.g. via
  // the Render blueprint in backend/render.yaml), then redeploy the frontend.
  // See README.md "Deployment" section.
  apiBase: 'https://aria-support-api.onrender.com',
};
